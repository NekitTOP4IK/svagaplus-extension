# Copy to .env and fill in values for local development.
# Prod values have built-in defaults; dev values are required for dev/firefox_dev targets.

# Production (defaults are baked into build.js, override only if self-hosting)
# BACKEND_URL_PROD=https://tributealerts.nekittop4ik.space
# BOT_USERNAME_PROD=tributealertsbot

# Development (required for dev/firefox_dev targets)
BACKEND_URL_DEV=
BOT_USERNAME_DEV=
